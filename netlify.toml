# Residency+ — build runs from base; env injection must run before publish.
[build]
  base = "prototypes/residency-plus"
  command = "npm install && node scripts/inject-env.js"
  publish = "."
  functions = "netlify/functions"

